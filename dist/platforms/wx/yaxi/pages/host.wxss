@import '../../icon/iconfont.wxss';
@import '../css/yaxi.wxss';


.yx-host {
  bottom: 0;
  bottom: constant(safe-area-inset-bottom);
  bottom: env(safe-area-inset-bottom);
}

.iphone-x {
  display: block;
  height: constant(safe-area-inset-bottom);
  height: env(safe-area-inset-bottom);
}
